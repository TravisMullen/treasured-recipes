'use strict';

describe('TreasuredRecipesApp.recipeView module', function() {

    beforeEach(module('TreasuredRecipesApp.recipeView'));

    describe('recipeView controller', function() {

        it('should ....', inject(function($controller) {
            //spec body
            var recipeViewCtrl = $controller('recipeViewCtrl');
            expect(recipeViewCtrl).toBeDefined();
        }));

    });
});
